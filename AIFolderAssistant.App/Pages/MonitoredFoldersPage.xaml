<Page
    x:Class="AIFolderAssistant.App.Pages.MonitoredFoldersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:AIFolderAssistant.Core.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <StackPanel Margin="24" Spacing="12" MaxWidth="860">
        <TextBlock Text="Monitored folders" FontSize="22" FontWeight="SemiBold" />
        <TextBlock Text="Choose which folders FolderMind watches. System folders can never be monitored."
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button Content="+ Add folder" Click="Add_Click" Style="{ThemeResource AccentButtonStyle}" />
            <Button Content="Refresh" Click="Refresh_Click" />
        </StackPanel>
        <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" Foreground="Red" />
        <ListView ItemsSource="{x:Bind ViewModel.Folders, Mode=OneWay}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:MonitoredFolderInfo">
                    <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                            CornerRadius="8" Padding="12" Margin="0,0,0,8">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{x:Bind Path}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                <TextBlock Text="{x:Bind Status}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                    <Run Text="Last scanned: " /><Run Text="{x:Bind LastScannedDisplay}" />
                                </TextBlock>
                            </StackPanel>
                            <Button Grid.Column="1" Content="Disable" Tag="{x:Bind}" Click="Toggle_Click" />
                            <Button Grid.Column="2" Content="Remove" Tag="{x:Bind}" Click="Remove_Click" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
