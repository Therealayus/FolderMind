<Page
    x:Class="AIFolderAssistant.App.Pages.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:AIFolderAssistant.Core.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <StackPanel Margin="24" Spacing="12" MaxWidth="860">
        <TextBlock Text="History" FontSize="22" FontWeight="SemiBold" />
        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button Content="Refresh" Click="Refresh_Click" />
            <Button Content="Clear history" Click="Clear_Click" />
        </StackPanel>
        <TextBlock Text="{x:Bind ViewModel.Message, Mode=OneWay}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        <ListView ItemsSource="{x:Bind ViewModel.Entries, Mode=OneWay}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:HistoryEntry">
                    <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                            CornerRadius="8" Padding="12" Margin="0,0,0,8">
                        <StackPanel Spacing="4">
                            <TextBlock>
                                <Run Text="{x:Bind OriginalName}" />
                                <Run Text=" → " />
                                <Run Text="{x:Bind NewName}" FontWeight="SemiBold" />
                            </TextBlock>
                            <TextBlock Text="{x:Bind FolderPath}" FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                            <Button Content="Undo rename" Tag="{x:Bind}" Click="Undo_Click"
                                    IsEnabled="{x:Bind CanUndo}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
